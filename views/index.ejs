<% layout('layout') %>

<h2>Recent Posts</h2>
<ul>
  <% posts.forEach(post => { %>
    <li>
      <a href="/posts/<%= post._id %>"><%= post.title %></a> by <%= post.author.username %>
    </li>
  <% }) %>
</ul>